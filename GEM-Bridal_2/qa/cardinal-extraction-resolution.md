Four main components are complete, separated and safely inside their source slots (see cardinal-source-geometry.json). Initial source-slot edge failures were detached small alpha speckles, not body clipping. Used existing hatch-pet extract_state components on four slots (waving only supplies frame count4), kept all original main-component pixels, no generated drawing or direction edits. Recompose the four normalized anchors and rerun unchanged cardinal edge validator. No thresholds relaxed.