# 逐行动作提取决策

running-right：先使用默认组件提取，结构检查通过。源图统一头部尺度、顶部位置约161，而交替步态的脚底约557/585；逐帧紧裁缩放会将自然抬脚阶段额外放大约7%，抹去原有脚底高度变化。改用技能内置 stable-slots 共用视口，保留源图姿态和统一缩放；重新检查通过。并非通过缩放角色补造动画。
jumping：默认组件紧裁会把122→181的头部高度与498→588的脚底高度分别归一化，削弱腾空并产生额外缩放。使用技能内置共用视口保留完整起跳、腾空、落地时序和统一角色比例；重新结构检查通过，最终视觉复核检查实际运动。
